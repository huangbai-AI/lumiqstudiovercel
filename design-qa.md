# LumiQ 首页方案三设计校验

## 对照证据

- 视觉源：`/Users/a1/.codex/visualizations/2026/08/29/lumiq-home-chapters-source/design-desktop.png`
- 正确基线：`http://127.0.0.1:3010/en`，提交 `2a3af3f984e5f36cf6262de834c97054b7ac7d9a`
- 本地实现：`http://127.0.0.1:3022/en`
- 桌面实现截图：`/Users/a1/.codex/visualizations/2026/08/29/lumiq-home-chapters-source/final-desktop-chapters.png`
- 手机实现截图：`/Users/a1/.codex/visualizations/2026/08/29/lumiq-home-chapters-source/final-mobile-chapters.png`
- 并排对照：`/Users/a1/.codex/visualizations/2026/08/29/lumiq-home-chapters-source/comparison-desktop-hero.png`
- 桌面视口：CSS `1440 × 900`；截图像素 `1425 × 891`；设备密度由应用内浏览器保持默认。
- 手机视口：CSS `390 × 844`；截图像素 `375 × 812`；设备密度由应用内浏览器保持默认。
- 视觉源像素：`724 × 2172`；并排对照已统一缩放到每侧 `720 × 450`，只比较首屏相同状态，不把分辨率差异当作设计问题。

## 比较历史

### 第一轮

- P2：桌面首屏标题下缘与正文距离不足；手机单品文字区过高，局部压住产品且按钮接近视口下缘。
- 修正：增加桌面标题与正文的有效间距；将手机正文改为更短的本地化内容；压缩手机文字节奏并把文字区下移，让产品完整露出、按钮保留在一屏内。

### 第二轮

- 桌面六层在 `1440 × 900` 下均为 `900px`，顶部位置为 `0/900/1800/2700/3600/4500`。
- 手机六层在 `390 × 844` 下均为 `844px`，顶部位置为 `0/844/1688/2532/3376/4220`。
- 直接访问 `#nest-15` 后目标楼层相对视口顶部为 `0`；按 `PageDown` 后 Ola 相对视口顶部为 `0`。
- 行动按钮实际地址为 `/en/prelaunch`，点击后进入现有预登记页。

## 五项视觉面检查

- 字体：原站页头与正文体系保留；章节标题与编号使用高对比衬线层级，手机端无截断。
- 间距：桌面与手机均是一屏一层，标题、正文、按钮不互相重叠；固定页头下内容留有安全距离。
- 色彩：珍珠白、深海军蓝、浅蓝章节号与橙色行动按钮和视觉稿一致；文字对比清楚。
- 图片：所有产品与家庭场景均为生成后的完整背景画面，没有代码拼接、透明边缘或裁切断层；桌面和手机使用独立画幅。
- 文案：英文、繁中、日文键完整一致；产品名称、入口及跨世代家庭定位清楚。

## 交互与运行

- 鼠标/触控板/手机滚动由强制吸附与 `scroll-snap-stop` 保证；键盘翻页与直接锚点均已实测。
- `prefers-reduced-motion` 会关闭平滑滚动和动画。
- 应用内浏览器未出现运行错误覆盖层；开发与生产日志均无页面运行错误。
- 生产构建通过；三种语言的首页、产品、品牌故事、套餐、媒体、FAQ、联系、法律和预登记路由均返回 `200`；旧产品路由保持 `308`，未知路由保持 `404`。

## 聚焦对照说明

首屏并排图用于核对页头、标题、产品群组、光带、留白和行动按钮；各单品与家庭章节另有桌面、手机逐层截图，因此无需再做更小区域裁切。

## Findings

没有剩余 P0、P1 或 P2 问题。仅保留一个 P3 差异：实现使用原站页头的实际尺寸，而视觉稿页头更薄；这是遵守“全站导航原样继承”的有意取舍。

final result: passed
